# Merged requirements for Dockerized webapp + mobile-api containers.
# Adds the deps that the live server has via separate pip installs but
# aren't pinned in the repo's top-level requirements.txt (FastAPI stack,
# Gemini client, OCR helpers).

# ---- from repo requirements.txt (pinned) ----
annotated-types==0.7.0
anthropic==0.86.0
anyio==4.13.0
blinker==1.9.0
certifi==2026.2.25
click==8.3.1
distro==1.9.0
docstring_parser==0.17.0
et_xmlfile==2.0.0
Flask==3.1.3
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
itsdangerous==2.2.0
Jinja2==3.1.6
jiter==0.13.0
MarkupSafe==3.0.3
numpy==2.4.3
openpyxl==3.1.5
pandas==3.0.1
psycopg2-binary==2.9.11
pydantic==2.12.5
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
rapidfuzz==3.14.5
scipy==1.17.1
six==1.17.0
sniffio==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
Werkzeug==3.1.6

# ---- mobile_api.py (FastAPI stack, JWT, password hashing) ----
fastapi
uvicorn[standard]
python-jose[cryptography]
passlib[bcrypt]
python-multipart

# ---- agent.py / chat.py / receipt scanner (Gemini) ----
google-generativeai

# ---- receipt_ocr.py subprocess (tesseract bindings + image libs) ----
pytesseract
Pillow
opencv-python-headless
